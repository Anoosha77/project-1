// import { axiosInstanceAdmin } from "../instances/instances";

// export const createProduct = (data: {
//   name: string;
//   price: number;
//   category_id: string;
//   user_id: string;
// }) => axiosInstanceAdmin.post("/create-product", data, { withCredentials: true });

// export const updateProduct = (id: string, data: {
//   name: string;
//   price: number;
//   category_id: string;
//   user_id: string;
// }) => axiosInstanceAdmin.put(`/update-product/${id}`, data, { withCredentials: true });

// export const deleteProduct = (id: string, deleted_id: string) =>
//   axiosInstanceAdmin.post(`/delete-product/${id}`, { deleted_id }, { withCredentials: true });
